algoritmo "conta_de_0_ao_infinito"
var
   valor, contador: inteiro
inicio
      contador <- 0
      escreva ("Quer contar até que número?")
      leia (valor)
      enquanto (contador <= valor) faca
      escreval (contador)
               contador <- contador + 1
      fimenquanto
fimalgoritmo